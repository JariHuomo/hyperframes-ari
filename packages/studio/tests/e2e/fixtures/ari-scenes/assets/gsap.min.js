// Fixture preparation replaces this sentinel with gsap/dist/gsap.min.js from
// the installed workspace dependency. Never render this template unprepared.
throw new Error("Prepare the Ari scene fixture with ari:test:scenes before rendering.");
